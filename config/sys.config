%% sys.config — substrate-L0 system configuration
%% Per ea-m1-r2 §3 deliverable 4 (snapshot + log compaction policy).
%% Per EA Ruling 2: use ra defaults at Phase A; revisit at organism-soak observation.

[
    %% ra defaults are applied automatically; no overrides at Phase A
    {ra, [
        %% Default snapshot interval: 4096 commands
        %% Default log compaction threshold: 4096 commands
        %% These are ra's built-in defaults — explicit setting omitted per EA Ruling 2.
        %% Revisit at Gate 3 organism-soak observation if mutation rate exceeds ~100/sec sustained.
    ]},
    {spine, [
        %% Spine app env populated from spine.app.src
    ]}
].
